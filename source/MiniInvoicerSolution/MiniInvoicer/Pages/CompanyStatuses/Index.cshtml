@page
@model MiniInvoicer.Pages.CompanyStatuses.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CompanyStatus[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CompanyStatus[0].Description)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.CompanyStatus) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.CompanyStatusID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.CompanyStatusID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.CompanyStatusID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
