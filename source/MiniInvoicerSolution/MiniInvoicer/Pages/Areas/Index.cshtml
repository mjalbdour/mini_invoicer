@page
@model MiniInvoicer.Pages.Areas.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Area[0].AreaDomainID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Area[0].ZipcodeID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Area[0].Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Area) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AreaDomainID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ZipcodeID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.AreaID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.AreaID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.AreaID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
